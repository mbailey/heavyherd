<h1>Listing venues</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Email</th>
    <th>Web</th>
  </tr>

<% for venue in @venues %>
  <tr>
    <td><%=h venue.name %></td>
    <td><%=h venue.address_id %></td>
    <td><%=h venue.email %></td>
    <td><%=h venue.web %></td>
    <td><%= link_to 'Show', admin_venue_path(venue) %></td>
    <td><%= link_to 'Edit', edit_admin_venue_path(venue) %></td>
    <td><%= link_to 'Destroy', admin_venue_path(venue), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New venue', new_admin_venue_path %>
